<nav class="sheet-tabs tabs" data-group="primary">
    {{#each tabs as |tab|}}
    <a class="{{tab.cssClass}} nav-tab" data-action="tab" data-group="{{tab.group}}" data-tab="{{tab.id}}">
        <i class="{{tab.icon}}"></i>
        <label>{{localize tab.label}}</label>
    </a>
    {{/each}}
</nav>