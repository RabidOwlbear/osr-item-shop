<form class="select-cont">
  <h3 class="header">{{localize "OSRIS.shopSelect.title"}}</h3>
  <div class="shop-cont">
    {{#if uShopActive}}
      <div class="row">
        <label>{{localize "OSRIS.shopSelect.universalShop"}}</label>
        <div class="shop-controls">
          <a
          class="universal-btn"
          data-actor-id="{{actorId}}"
          title="{{localize "OSRIS.shopSelect.openUniversal"}}"
          >
            <i class="fa-solid fa-store"></i>
          </a>
        </div>
      </div>
    {{/if}}
    {{#each shops as |shop idx|}}
      <div class="row">
        <label for="">{{shop.name}}</label>
        <div class="shop-controls">
          <a class="shop-btn" data-shop-id="{{shop.id}}" data-actor-id="{{../actorId}}">
            <i class="fa-solid fa-store"></i>
          </a>
        </div>
      </div>
    {{/each}}
  </div>
</form>